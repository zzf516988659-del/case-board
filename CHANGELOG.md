# Changelog

本项目所有显著变更都会记录在此文件。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/),版本号遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/)。

> 开源版自 v0.3.9 起重新整理变更记录,早期内部迭代历史不在此公开仓列出。

---

## [0.3.19] - 2026-06-16

> 滴答清单接入改用「API 口令」(免注册开发者应用)+ 双人办案案件资料包合并 + 一批 Windows 反馈修复。
> 感谢 [@zzf516988659-del](https://github.com/zzf516988659-del) 在 Windows 上的测试与多个 PR / issue。

### ✨ 新增
- **案件资料包合并(双人办案)**:法律工具页新增「导出案件资料包(.zip)/ 导入合并」。两位律师
  各自有同一案件的部分材料,一方导出、另一方导入合并进同一案件 —— 材料按内容去重、取并集,
  只增不删、不覆盖、可重复合并不翻倍,天然无冲突;按案号自动建议合并目标。

### 🔧 变更
- **滴答清单同步改用「API 口令」**:鉴权从 OAuth(需自己注册开发者应用)改成滴答设置里一键生成的
  「API 口令」(免费用户也可用),接入从「注册应用 + 浏览器授权」降到「粘贴一个口令」;连接页内置
  获取口令的图文步骤。看板加的待办不填日期默认「今天」,在手机滴答「今天」页直接可见。
- **MiniMax 模型选择改下拉**,新增 M2.7-highspeed 档。

### 🐛 修复(感谢 @zzf516988659-del 反馈)
- 导入校验按云端后端路由:MiniMax 用户点「导入」不再被误报缺 DeepSeek Key。
- 「沉淀为办案经验」修复:默认知识库路径 `~/...` 未展开导致恒判路径不存在。
- 本地知识库索引:支持用户自带分类目录结构(原只扫默认结构会扫到 0 文件);切换知识库根目录后
  自动清空旧索引,不再「搜到不存在的文件」。
- 案件合并文件名跨平台清洗,避免 Windows 端因文件名非法字符静默丢文件。

### 📄 文档
- README 补 Windows 构建避坑(回应 issue #2)。

## [0.3.18] - 2026-06-15

### 🐛 修复
- **根治升级后启动闪退**:部分用户从旧版升级后,数据库迁移校验失败导致启动崩溃。新增启动期
  校验值对齐(不改 SQL、不动数据),根治此问题。**已经卡在升级后闪退的用户,请手动下载本版重装。**
- **Windows 适配**:① 设置页「申请 API」按钮、「打开源文件」「在文件夹中显示」原仅 macOS 可用,改为跨平台;
  ② 反馈按钮在 Windows 上无法保存(桌面路径解析),已修。

### 🆕 新增
- **MiniMax 云端模型**:设置页新增「云端 AI 后端」,可在 DeepSeek / MiniMax 之间切换,模型名可编辑
  (感谢 **Pure小法师** 提出建议并提供接入资料)。
- **Windows 缺 WebView2 运行时引导**:启动时若系统未安装 WebView2,弹窗引导联网下载,不再无声闪退。

## [0.3.17] - 2026-06-15

### 🆕 本地法律向量检索(法条 / 案例 / 企业)
- AI 查法条 / 类案改走**语义检索本地知识库**:整部法律按「第X条」切片建向量索引,直接命中相关条文,命中后从本地取全文,省专业检索调用。
- 索引覆盖三块:法条(整部法律全文,自动去重多副本、排除已废止法)、案例(裁判文书 + 办案经验)、企业(企业档案)。
- 自动增量索引:出报告 / 启动 / AI 对话后,后台把新内容补进索引。设置页「法律向量检索」可查看规模、手动重建、开关自动维护。需配 embedding(OpenAI 兼容,如硅基流动 bge-m3 免费)。

### 🆕 首页日程日历视图
- 首页新增日历视图:开庭 / 续封 + 带日期待办 + 手动提醒,按月汇总查看排期。

### 💄 体验
- 设置页 provider 卡更紧凑;设置页与更新提示加入交流群二维码;首页排序控件细节打磨。

## [0.3.16] - 2026-06-14

### 🆕 首页案件列表视图 + 排序 / 筛选 / 多选(感谢 @robieepeng 贡献,本项目首个外部 PR)
- 首页新增列表视图,与卡片视图并存,右上角一键切换;批量案件多时不必在一堆卡片里翻找。
- 支持按状态 / 金额 / 立案时间 / 开庭时间排序,按案件状态 / 法院筛选,以及多选 / 全选 / 反选 / 清空。
- 完整保留卡片拖拽排序与已结案自动沉底逻辑(仅默认网格视图生效)。

### 🆕 云端 OCR 单文档进度反馈
- 大图扫描件(如长截图,无文本层)云端排队 / 识别可达数分钟,此前进度条停在某文件不动易被当成卡死;现在文件名下方实时显示「云端排队中 / 识别中(已 N 秒)」。

### 🐛 修复
- 结案沉淀的办案经验卡片此前本地知识库检索默认搜不到 → 已纳入默认检索范围,办同类案件可复用。

## [0.3.15] - 2026-06-13

### 🆕 深度分析模式(鉴定式请求权基础分析)
- AI 助手新增「深度分析」:用请求权基础 + 鉴定式方法论对案件做结构化深度分析,分四阶段、两道闸停下来跟你确认(候选请求权清单 → 分析大纲)后逐要件论证,落一份《深度分析报告》;聚焦诉讼请求背后的实体争点。
- 推理阶段实时显示「深度推理中(N 字)」,看得出在思考而非卡住;需你拍板的问题统一弹可点选项卡,不再用文字让你手输。

### 🆕 其他
- **结案沉淀办案经验**:结案案件一键提炼成结构化经验卡片(争议焦点 / 裁判规则 / 法条适用 / 办案心得)写入本地知识库,办类似案件时可检索复用。
- **导出 Word 标点规范化**:导出文书时中文语境的半角标点自动转全角;金额 / 案号 / 英文 / 时间等内容锁定不动。
- **结案自动归档 + 每案待办清单**:结案案件首页自动沉底、手动状态不再被 AI 覆盖;每个案件可建待办清单(打钩消失),首页汇总跨案件未完成待办。
- **OCR 优化**:批量重识别不再每个都自动跑分析;带水印工商调档件可「去水印重新识别」。
- 报告与 AI 助手按「我方代理立场」(原告方 / 被告方)调整侧重;团队改为本地案件的只读镜像 + 备注。

---

## [0.3.14] - 2026-06-12

### 🆕 应用内自动更新
- 发现新版本后可「立即更新」:应用内下载(带进度条)→ 自动安装 → 重启,不用再手动下载覆盖。
- **安全**:每个更新包都由作者私钥签名,App 内置公钥强制验签,签名不对直接拒装;私钥不在开源代码内,仅作者本机 + CI 加密 secret 持有。即便源码公开、下载站被攻破或遭中间人,攻击者拿不到私钥就签不出能装的包。
- 应用内更新不可用 / 失败时自动回退到手动下载;升级重启后会提示本次更新内容。
- 注:本次需手动更新一次(旧版没有更新组件),从 0.3.14 起后续版本才能一键更新。

---

## [0.3.13] - 2026-06-12

### 🆕 新增(0.3.10 ~ 0.3.13 累积同步)

- **OCR 双线路**:新增 PaddleOCR VL-1.6(百度 AI Studio)云端识别,设置页填访问令牌(免费 20,000 页/天)即自动成为 MinerU 的备用线路 — 失败 / 排队超时 / 额度用完自动切换;也可一键设为主力。不填则一切照旧。
- **审级模型**:一个案件可记多个审级(仲裁 → 一审 → 二审 → 再审),各审级独立案号 / 承办机关 / 当事人称谓;首页显示最新审级,详情页新增「审级历程」;案件状态新增「仲裁中」「再审中」。
- **团队版(无服务器)**:创建团队 → 配对码加入 → 同一办公网内自动互相同步案件进度(接力传播);权限逐人配置,有编辑权可改队友案件状态,改动留痕可撤销。只同步案件登记信息,不传文档原文。
- **外部数据源接入(MCP)**:元典 / 企查查等平台的云端 MCP 服务可整段粘贴配置接入 AI 助手。
- **还款自动入账**:银行转账截图识别出还款后自动记进执行页还款记录(带 AI 标记,可删)。
- 法院短信可按当事人姓名匹配案件;案件详情页新增「重新分析」按钮;全案分析阶段显式进度提示,失败透传真实错误。
- **Windows 支持**:安装包内置 WebView2 引导器(构建工作流见 `.github/workflows`)。

### 🔧 变更

- 更新提醒每个新版本只弹一次;详情页手改信息首页同步更新。

### 🆕 新增

- **Word 导出排版升级**:统一到原生 OOXML 排版引擎,表格不再散架,支持仿宋 / 黑体 / 方正小标宋字体。
- **一个文件夹自动识别为多个案件**:拖入文件夹时自动检测,提供拆分预览确认,共用材料挂到每个案件。

### 🔧 变更

- 文档导出抬头补回案号 / 法院 / 案由等元信息。

### ⚠️ 已知限制

- 共用材料目前对每个案件各抽取一次(尚未做抽取结果复用)。
